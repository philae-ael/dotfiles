#!/bin/sh

#redifine echo to haven't to precise -n
function echo() {
    /bin/echo -n $@
}

echo "%{A4:pulseaudio-ctl up:}%{A5:pulseaudio-ctl down:}%{A:pulseaudio-ctl mute:}Volume : $(pulseaudio-ctl full-status| sed 's/\([0-9]\+\).\+/\1/') %{A}%{A}%{A}"

